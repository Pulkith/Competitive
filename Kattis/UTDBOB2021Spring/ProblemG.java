
import java.util.ArrayList;

public class ProblemG {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);

        int row = s.nextInt(), col = s.nextInt(), pass = s.nextInt();
        int grid[]][] = new int[row][col];

        for(int i = 0; i < row; ++i) {
            for(int j = 0; j < col; ++j)
                grid[i][j] = s.nextInt();
        }
        
        
    }
}
